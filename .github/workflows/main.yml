name: Rust

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Test
      env: 
        REDDIT_USERNAME: zower98
      run: cargo test -- --nocapture
    - name: Format
      run: cargo fmt -- --check 